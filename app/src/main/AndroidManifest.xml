<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smart_adviser_for_wellbeing">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartAdviserforWellbeing">
        <activity android:name=".ViewReport" />
        <activity android:name=".WelcomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ReadingLinkCP" />
        <activity android:name=".ReadingStressCP" />
        <activity android:name=".ReadingAngryCP" />
        <activity android:name=".ReadingDepressionCP" />
        <activity android:name=".ReadingContentPage" />
        <activity android:name=".FeelingQ" />
        <activity android:name=".NoteDetailActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>